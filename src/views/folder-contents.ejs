<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <script src="/script.js"></script>
  <title><%= title %></title>
</head>
<body>
  <header>
    <h1>File Manager</h1>
    <%- include('partials/navbar') %>
  </header>
  <main>
    <h1><%= title %></h1>

    <a href="/upload">< Back</a>

    <button onclick="toggleEdit()">Edit</button>

    <form class="rename-folder" action="/folder/<%= folderId %>/update-folder" method="post">
      <input type="text" name="folderName" id="folderName" value="<%= locals.title %>">
      <button type="submit">Update</button>
    </form>

    <a href="/folder/<%= folderId %>/delete-folder">Delete</a>

    <form action="/folder/<%= folderId %>" method="post" enctype="multipart/form-data">
      <input type="file" name="fileupload">
      <button type="submit">Upload</button>
    </form>
    
    <% if (locals.files) { %>
      <% files.forEach(file => { %>
        <a href="/folder/<%= folderId %>/<%= file.id %>"><%= file.name %></a>
        <%= file.createdAt %>
        <%= file.fileSize %>
      <% }) %>
    <% } %>
  </main>
</body>
</html>